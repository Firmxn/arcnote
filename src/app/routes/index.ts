/**
 * Routes Configuration
 * Placeholder untuk routing (akan diimplementasikan dengan React Router jika diperlukan)
 */

export const routes = {
    home: '/',
    page: (id: string) => `/page/${id}`,
};
